#include <bits/stdc++.h>
using namespace std;

const int MOD = 1e8 + 7;

int solve(int n, int k)
{
    int ans = 1;
    vector<int> dp(n + 1);
    dp[0] = 1;
    return ans;
}
int main()
{
    cin.tie(nullptr)->sync_with_stdio(false);

    int n, k;
    cin >> n >> k;

    cout << solve(n,k);
    return 0;
}